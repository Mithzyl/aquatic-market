# 移动端样式重构任务分解

## 项目进度说明
- **目标**：重构整个前端样式，使其完全符合移动端使用体验
- **优先级**：P0（最高优先级）
- **预计工作量**：中等

## 任务清单

### [ ] 任务1：创建移动端底部导航组件
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 创建 `src/components/MobileNav.tsx` 组件
  - 实现底部固定导航栏（参考主流App设计）
  - 包含首页、商品、购物车、订单、我的五个入口
  - 当前页面高亮显示
  - 适配安全区域（iPhone刘海屏、底部手势条）
  - 使用海洋主题配色
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `human-judgment` TR-1.1：导航栏在移动端底部固定显示
  - `human-judgment` TR-1.2：当前页面图标高亮正确
  - `programmatic` TR-1.3：点击跳转功能正常
- **文件**：
  - 创建：`src/components/MobileNav.tsx`
  - 修改：`src/App.tsx`（集成底部导航）

### [ ] 任务2：重构全局样式系统
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 更新 `src/index.css` 添加移动端基础样式
  - 设置移动端字体大小基准（16px避免iOS缩放）
  - 添加移动端安全区域适配（env(safe-area-inset-*)）
  - 优化移动端间距系统
  - 添加移动端触摸反馈样式
  - 优化玻璃卡片效果在移动端的表现
- **验收标准**：AC-3, AC-4
- **测试要求**：
  - `programmatic` TR-2.1：正文字体 >= 16px
  - `human-judgment` TR-2.2：移动端显示无缩放问题
  - `human-judgment` TR-2.3：安全区域适配正确
- **文件**：
  - 修改：`src/index.css`

### [ ] 任务3：重构Header组件（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/components/Header.tsx` 移动端显示
  - 移动端Logo和标题缩小
  - 导航链接改为汉堡菜单或隐藏
  - 购物车图标优化，确保点击区域 >= 44px
  - 适配移动端顶部安全区域
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `human-judgment` TR-3.1：Header在移动端显示正常
  - `programmatic` TR-3.2：所有点击区域 >= 44px
- **文件**：
  - 修改：`src/components/Header.tsx`

### [ ] 任务4：重构首页（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/pages/Home.tsx` 移动端布局
  - Hero区域文字大小适配移动端
  - 精选商品网格改为移动端2列或单列
  - 新鲜保障和客户评价区域适配移动端
  - 按钮高度 >= 48px
  - 图片比例适配移动端
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-4.1：首页在移动端显示正常
  - `human-judgment` TR-4.2：文字清晰可读
  - `programmatic` TR-4.3：按钮点击区域充足
- **文件**：
  - 修改：`src/pages/Home.tsx`

### [ ] 任务5：重构商品列表页（移动端适配）
- **优先级**：P0
- **依赖**：任务1, 任务2
- **描述**：
  - 优化 `src/pages/Products.tsx` 移动端布局
  - 移动端隐藏左侧分类导航（使用底部导航）
  - 商品网格改为移动端2列或单列
  - 搜索框高度 >= 48px，字体 >= 16px
  - 分类筛选按钮优化移动端显示
  - 商品卡片全宽显示
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-5.1：商品列表在移动端显示正常
  - `human-judgment` TR-5.2：搜索和筛选操作便捷
  - `programmatic` TR-5.3：输入框避免自动缩放
- **文件**：
  - 修改：`src/pages/Products.tsx`

### [ ] 任务6：重构商品卡片组件（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/components/ProductCard.tsx` 移动端显示
  - 移动端卡片全宽显示
  - 图片比例调整为 16:9 或 4:3
  - 文字大小适配移动端
  - 按钮和点击区域 >= 44px
  - 促销标签位置优化
  - 减少移动端动画效果
- **验收标准**：AC-1, AC-2, AC-3, AC-4
- **测试要求**：
  - `human-judgment` TR-6.1：商品卡片在移动端显示清晰
  - `programmatic` TR-6.2：所有点击区域 >= 44px
  - `human-judgment` TR-6.3：图片加载和显示正常
- **文件**：
  - 修改：`src/components/ProductCard.tsx`

### [ ] 任务7：重构商品详情页（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/pages/ProductDetail.tsx` 移动端布局
  - 图片轮播适配移动端手势
  - 商品信息垂直布局优化
  - 数量选择器移动端友好
  - 按钮高度 >= 48px
  - 底部固定加入购物车按钮（可选）
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-7.1：商品详情页在移动端显示正常
  - `human-judgment` TR-7.2：数量选择操作便捷
  - `programmatic` TR-7.3：按钮点击区域充足
- **文件**：
  - 修改：`src/pages/ProductDetail.tsx`

### [ ] 任务8：重构购物车页面（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/pages/Cart.tsx` 移动端布局
  - 商品列表项垂直布局优化
  - 数量调整按钮增大
  - 支持左滑删除商品（手势）
  - 底部固定结算栏
  - 配送方式选择优化
  - 表单输入框高度 >= 48px
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-8.1：购物车页面在移动端显示正常
  - `human-judgment` TR-8.2：数量调整和删除操作便捷
  - `programmatic` TR-8.3：输入框避免自动缩放
- **文件**：
  - 修改：`src/pages/Cart.tsx`

### [ ] 任务9：重构订单页面（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/pages/Orders.tsx` 移动端布局
  - 订单卡片垂直布局优化
  - 订单状态标签优化显示
  - 操作按钮（支付、取消）增大
  - 订单详情展开/收起功能
  - 空状态优化
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-9.1：订单页面在移动端显示正常
  - `human-judgment` TR-9.2：订单信息清晰可读
  - `programmatic` TR-9.3：操作按钮点击区域充足
- **文件**：
  - 修改：`src/pages/Orders.tsx`

### [ ] 任务10：重构用户中心页面（移动端适配）
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 优化 `src/pages/User.tsx` 移动端布局
  - 用户信息卡片优化
  - 会员等级和积分显示优化
  - 登录表单输入框高度 >= 48px
  - 按钮高度 >= 48px
  - 适配移动端键盘弹出
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-10.1：用户中心在移动端显示正常
  - `human-judgment` TR-10.2：登录表单操作便捷
  - `programmatic` TR-10.3：输入框避免自动缩放
- **文件**：
  - 修改：`src/pages/User.tsx`

### [ ] 任务11：优化移动端动画性能
- **优先级**：P1
- **依赖**：任务1-10
- **描述**：
  - 减少移动端复杂动画
  - 使用 CSS transform 替代 position 变化
  - 添加 will-change 优化
  - 图片懒加载实现
  - 减少重绘和重排
- **验收标准**：AC-4
- **测试要求**：
  - `programmatic` TR-11.1：首屏加载时间 < 3秒
  - `human-judgment` TR-11.2：动画流畅无卡顿
- **文件**：
  - 修改：`src/index.css`
  - 修改：各组件动画相关代码

### [ ] 任务12：移动端兼容性测试
- **优先级**：P1
- **依赖**：任务1-11
- **描述**：
  - iOS Safari 测试
  - Android Chrome 测试
  - 不同屏幕尺寸测试
  - 横竖屏切换测试
  - 键盘弹出测试
  - 底部导航栏安全区域测试
- **验收标准**：AC-5
- **测试要求**：
  - `human-judgment` TR-12.1：iOS Safari 显示正常
  - `human-judgment` TR-12.2：Android Chrome 显示正常
  - `human-judgment` TR-12.3：安全区域适配正确
- **文件**：
  - 无需修改代码，仅测试

## 任务依赖关系

```
任务2 (全局样式)
    ↓
任务1 (底部导航) → 任务3 (Header) → 任务4 (首页)
    ↓                ↓                ↓
任务5 (商品列表) → 任务6 (商品卡片) → 任务7 (商品详情)
    ↓
任务8 (购物车) → 任务9 (订单) → 任务10 (用户中心)
    ↓
任务11 (性能优化) → 任务12 (兼容性测试)
```

## 优先级说明

- **P0**：核心功能，必须完成
- **P1**：重要功能，应该完成
- **P2**：增强功能，条件允许时完成

## 当前状态

- ⏳ 任务1-12：待完成
